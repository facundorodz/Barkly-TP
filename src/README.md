# Sobre src.
En esta carpeta va todo el código del **home** y dentro de la carpeta **pages** va una carpeta nueva con cada sub-página, por ejemplo:

> Pages
>> Login
>>> login.html
>>> login.css
>>> login.js 